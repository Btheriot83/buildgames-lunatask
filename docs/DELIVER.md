# Deliver — Tideglass Phase A cuts + hand copy

## Technique 6 — Cut (subtractive)
1. **Removed glassmorphism** — all `backdrop-filter` / frosted cards → opaque paper/ink surfaces.
2. **Fraunces demoted** — display font only on brand wordmark; card titles → IBM Plex Sans.
3. **Stat pills demoted** — no “banner” energy; quieter desk meters or removed from chrome if noise.
4. **Sample/Import/Clear demoted** — utility behind quieter actions; Export stays honest backup.
5. **No fake encryption / sync / accounts** — never shipped.
6. **Empty text-only voids cut** — replaced with Imagine desk still (+ drift video).
7. **Emoji / marketing FAQ / 3-icon row / fake 10K stats** — never added.
8. **Gradient CTAs / glow shadows** — removed where present.

## Technique 8 — Hand copy (before → after)
| Location | Before (AI/generic) | After (hand) |
|----------|---------------------|--------------|
| Eyebrow | `Local ledger` | `Harbor desk` |
| Sub | `Tasks · habits · mood — stays on this device` | `Today’s water level — tasks, habits, mood. Stays on this device.` |
| Focus empty | `Clear water — no open tasks due this day.` | `No slips due. Capture one, or pull an AI plan.` |
| Habits empty | `Nothing due on this day.` | `No habits due on this tide.` |
| Mood lead | `How is the tide today?` | `Name the weather in one line.` |
| Mood CTA | `Log check-in` | `Save today’s weather` |
| AI plan CTA | (none) | `Plan my day` |
| AI apply | (none) | `Add plan to Today` |

## Kept for the one job
Today board, day strip, tasks, habit streaks, mood journal, JSON export, AI `/api/daily-plan`.
