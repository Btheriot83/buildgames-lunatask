# specify-the-look (catalog prompt, filled — identity LOCKED)

Catalog prompt (do not invent): “Do not use your default aesthetic. Use these constraints: Fonts / Colors / Mood references. Propose 3 font-and-color pairings that fit, then wait for me to pick before designing screens.”

Identity is already picked. Do not reseed.

## Constraints
- Fonts: Fraunces (mark-only wordmark). IBM Plex Sans (UI). IBM Plex Mono (time / meta).
- Colors: ink `#0b1c24` field, bone paper `#f4efe6`, ink-on-paper `#0e1a20`, seafoam `#2f7f78`, coral `#c45b4e`, brass `#a8893e`. Flat fills only — no gradient chrome, glass, vibe-purple.
- Mood references: Things 3 Today/Upcoming quiet grouping (one list, editorial headings, circular-calm checks, This Evening). Structured one-day timeline confidence (the day is the product, now-marker, hour rhythm). Harbor Desk blotter stays.

## Three pairings (locked pick)
1. **LOCKED — Harbor Desk.** Fraunces mark + Plex Sans + ink/bone/seafoam. Things grouping on paper, not iOS white.
2. Rejected: Newsreader + Source Sans + cool slate (reads SaaS, not harbor).
3. Rejected: IBM Plex Serif as page default (Fraunces-everywhere tell).

Execute pairing 1. Today is the job.
